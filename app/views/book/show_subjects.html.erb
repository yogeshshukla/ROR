<strong>Subject: </strong> <%= link_to @subject.name,
:action => "show_subjects", :id => @subject.id %><br />

<ul>
   <% @subject.books.each do |c| %>
   <li><%= link_to c.title, :action => "show", :id => c.id -%></li>
   <% end %>
</ul>